## Getting Started

Install the dependencies:

```bash
npm install
```

Setup the environment variables:

```bash
cp .env.example .env
```

Build the application:

```bash
npm run build
```

Serve the application (after building):

```bash
npm run start
```

Run the development server:

```bash
npm run dev
```
